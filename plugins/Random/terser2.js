// Klo mau pake, pake aja ini bkn enc cma terser aja

import{minify}from"terser";const handler=async(m,{conn:conn,args:args})=>{if(0===args.length)return conn.reply(m.chat,"Mohon sertakan kode yang akan diompresi.",m);const inputCode=args.join(" "),options={mangle:!1,compress:!0};try{const result=await minify(inputCode,options);result.error?conn.reply(m.chat,"Terjadi kesalahan dalam mengompresi kode.",m):conn.reply(m.chat,result.code,m)}catch(error){conn.reply(m.chat,"Terjadi kesalahan dalam mengompresi kode.",m)}};handler.command=/^terser2$/i;export default handler;